import 'package:flutter/material.dart';

const colorAppScheme = Color.fromRGBO(139, 195, 74, 1);

//silverBar
const iconsColor = Color(0xFF08A652);
const tabBarColor = Color(0xFF068441);
const labelTabBarColor = Color(0xFF000000);

//subscribes
const subscribeCardBackground = Color(0xFFFFFFFF);
const firstBoxShadow = Color.fromRGBO(0, 0, 0, 0.08);
const secondBoxShadow = Color.fromRGBO(79, 79, 108, 0.07);
const secondaryTextColor = Color.fromRGBO(0, 0, 0, 0.55);

//rates
const dividerColor = Color.fromRGBO(0, 0, 0, 0.12);

//interests
const chipBorderColor = Color(0xFFFFFFFF);
const chipBackgroundColor = Color.fromRGBO(0, 0, 0, 0.08);
